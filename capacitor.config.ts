import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.mohammad.malekzad.ketabyablite',
  appName: 'EnglishBookDownloaderLite',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
